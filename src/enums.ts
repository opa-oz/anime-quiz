export enum Answers {
    FIRST = 1,
    SECOND = 2,
    THIRD = 3,
    FOURTH = 4
}

export enum Commands {
    AGREE,
    DISAGREE,
    EASY,
    HARD,
    HELP,
}

export enum Difficulty {
    EASY,
    HARD,
}
